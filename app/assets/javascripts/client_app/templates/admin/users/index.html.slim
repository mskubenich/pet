div
  h2 Users
  table.table.table-striped.table-responsive
    thead
      tr
        th
        th
          | Email
        th
          | Login
        th
          | Phone
        th
          | Organizations
        th
          | Created At
        th
          | Actions
    tbody
      tr ng-repeat="user in users"
        td
          img ng-src="{{user.avatar_url}}" style="width: 50px;"
        td
          | {{ user.email }}
        td
          | {{ user.login }}
        td
          | {{ user.phone }}
        td
          span.black-budget ng-show="user.scorp"
            | СКОРП
          span.black-budget ng-show="user.rkf"
            | РКФ
        td
          | {{ user.created_at }}
        td
          a
            .i.fa.fa-close ng-click="$parent.destroyInvite(invite)"